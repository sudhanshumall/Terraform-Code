// Define variable
def branch = env.GIT_BRANCH

pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
               sh '''
                    printenv
                    pwd
                    ls -la
                    rm -rf /var/jenkins_home/${branch}
                    mkdir -p /var/jenkins_home/${branch}
                    
                    '''
            }
        }
    }
}