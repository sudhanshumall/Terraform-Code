pipeline {
    agent {
    node {
      customWorkspace '/var/jenkins_home/'
    }
  }

    stages {
        stage('Build') {
            steps {
                echo 'Building..'
            }
        }
    }
}